@page "/register"
@rendermode InteractiveServer
@layout AuthLayout


<PageTitle>Catatan - Register</PageTitle>

<EditForm Model="@_registerFormModel" OnValidSubmit="@HandleSubmit" class="d-flex flex-column justify-content-center align-items-center gap-3 w-50">
    <DataAnnotationsValidator />

    <div class="w-100 d-flex  flex-column justify-content-center align-items-center">
        <label class="form-label w-50 text-lg-start">
            Name:
        </label>
        <InputText @bind-Value="_registerFormModel.Name" class="form-control w-50" />
        <ValidationMessage For="@(() => _registerFormModel.Name)" />
    </div>

    <div class="w-100 d-flex  flex-column justify-content-center align-items-center">
        <label class="form-label w-50 text-lg-start">
            Email:
        </label>
        <InputText @bind-Value="_registerFormModel.Email" class="form-control w-50" />
        <ValidationMessage For="@(() => _registerFormModel.Email)" />
    </div>

    <div class="w-100 d-flex flex-column justify-content-center align-items-center">
        <label class="form-label w-50 text-lg-start">
            Password:
        </label>
        <InputText @bind-Value="_registerFormModel.Password" type="password" class="form-control w-50" />
        <ValidationMessage For="@(() => _registerFormModel.Password)" />
    </div>

    <div class="w-100 d-flex flex-column justify-content-center align-items-center">
        <label class="form-label w-50 text-lg-start">
            Confirm Password:
        </label>
        <InputText @bind-Value="_registerFormModel.ConfirmPassword" type="password" class="form-control w-50" />
        <ValidationMessage For="@(() => _registerFormModel.ConfirmPassword)" />
    </div>

    <div class="w-100 d-flex justify-content-center gap-2">
        <button type="submit" class="btn btn-primary">Register</button>
        <button class="btn btn-secondary">
            <NavLink class="nav-link" href="">
                Login
            </NavLink>
        </button>
    </div>

</EditForm>
